# Railway configuration for the standalone Backend repo
[build]
dockerfilePath = "Dockerfile"

[deploy]
# We're omitting startCommand here to allow the Dockerfile's CMD ["python", "run.py"] 
# to take control, which correctly handles the $PORT environment variable.
healthcheckPath = "/health"
restartPolicyType = "on_failure"
